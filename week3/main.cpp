#include<iostream>
#include<cmath>
using namespace std;


char popQuiz(double grade, string name) {

    char grades[] = {'a', 'b', 'c', 'd', 'f'};
    return 'A';

}

int area(double x, double y){
    x = sin(x);
    y = sin(y);

    return x + y;
}

int main() {

    double x = 4;
    double y = 2;

area(x,y);
cout << area(x,y);

    return 0;
}